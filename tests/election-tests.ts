import ElectionService from "../src/services/election-service";

describe("Election service unit tests", () => {
  describe("Get", () => {
    test("Should retrieve a valid election", async () => {
      // @TODO write tests
    });
  });
});
